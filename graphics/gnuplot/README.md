## Overview

Gnuplot version of "Hello, World!". Gnuplot is a portable command-line driven
graphing utility. This example renders "Hello, World!" as a label on a blank
canvas and writes the result to `build/hello.png`.

Build with `make`. The output will be in the `build` directory.

## Dependencies

```
gnuplot
make
```
