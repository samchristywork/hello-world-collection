## Overview

This is the ncurses version of "Hello, World!". ncurses is a library for
building terminal user interfaces. This example demonstrates colored text output,
reading keyboard input, displaying window dimensions, and handling terminal
resize events via `SIGWINCH`.

Press `q` to exit.

It can be built with `make`.

## Dependencies

```
gcc
libncurses-dev
make
```
