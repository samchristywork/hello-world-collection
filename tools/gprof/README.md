## Overview

gprof version of "Hello, World!". The program computes `fib(35)` to give the
profiler something to measure. Build with `make`, then run `make profile` to
execute the binary (producing `gmon.out`) and display the gprof flat profile.

## Dependencies

```
gcc
gprof
make
```
