## Overview

nm version of "Hello, World!". Build with `make` and run `make symbols` to
print the symbol table of the compiled binary. The source defines a global
string, a function, and `main` so the output includes multiple interesting
symbol types.

## Dependencies

```
gcc
make
binutils (provides nm)
```
