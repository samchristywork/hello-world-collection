## Overview

perf version of "Hello, World!". The program sums one billion integers to give
`perf stat` meaningful counters to report. Build with `make` and run `make stat`
to see hardware performance counters including cycles, instructions, and cache
statistics.

## Dependencies

```
gcc
make
perf
```
