## Overview

GDB version of "Hello, World!". Build with `make` and start a debugging session
with `make debug`. The program has a `greet()` function to demonstrate setting
breakpoints and inspecting the call stack.

## Dependencies

```
gcc
gdb
make
```
