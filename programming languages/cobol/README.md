## Overview

COBOL version of "Hello, World!". This example uses GnuCOBOL (`cobc`) to
compile and run COBOL source code. Build with `make` and run with
`./build/hello`.

## Dependencies

```
gcobol
make
```
