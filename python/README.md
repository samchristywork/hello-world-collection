## Overview

Python version of "Hello, World!". Run with `./hello.py`.

## `venv` Setup

Must have `python3-venv` installed.

```
python3 -m venv .
. bin/activate
```

## Dependencies

```
python3
python3-venv
```
