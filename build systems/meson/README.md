## Overview

Meson version of "Hello, World!". Meson generates Ninja build files by default,
making builds fast and incremental. Run the following commands to compile the
program:

```
meson setup build/
cd build/
ninja
```

## Dependencies

```
meson
ninja
gcc
```
