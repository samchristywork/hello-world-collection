## Overview

SCons version of "Hello, World!". SCons uses a Python-based `SConstruct` file
as its build description, and automatically tracks dependencies without a
separate `clean` step being required for most changes. Build with:

```
scons
```

## Dependencies

```
scons
gcc
```
